const bot = require('./../bot.js');
const igracias = require('./../template/igracias.js');
const request = require('request'); //HTTP Request

module.exports = {
  profile: function (replyToken, userId) {
    
  }
};
